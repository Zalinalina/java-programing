package day33_arrays;
import java.util.*;
public class GroupFriends {
    public static void main(String[] args) {
        String[] groupNames = {"Mindy", "Svetlana", "Ainur", "Milos", "Bahar", "Mike", "Ahmat", "Ziba"};
        // String names = groupNames[0];
        for (String each : groupNames) {

            System.out.println("Happy Holidays " + each + " !");
        }
    }
}